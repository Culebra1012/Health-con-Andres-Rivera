# Copia este archivo como .env.local y pega tu clave de OpenAI.
# La clave vive SOLO en el servidor — nunca se expone al navegador.
# Consíguela en https://platform.openai.com/api-keys

OPENAI_API_KEY=sk-...
